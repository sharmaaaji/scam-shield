# .NET
Api/bin/
Api/obj/
*.user

# Local secrets (real key lives in dotnet user-secrets, never here)
Api/appsettings.Development.json

# OS cruft
.DS_Store

# Eval run output (regenerated per run; the dataset itself IS tracked)
eval/results.json
